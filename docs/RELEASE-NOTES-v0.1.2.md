# Warren Buffett Agent v0.1.2

This release makes the standalone Agent installable from the Agent a user
already has, without presenting Codex as the only host.

## One-sentence install

```text
Install MrMaii/Warren-Buffett-Agent as a user-level Agent Skill for this agent. Inspect it first, use the matching host, and verify it is available.
```

## Included

- A minimal bilingual README that leads with installation, first use, and the
  qualification boundary before professional analysis.
- A smooth `960x640` Editorial Prompt installation motion covering common Agent
  Skills-compatible hosts.
- A discoverable `warren-buffett-agent` Skill containing the full 47-file Agent
  package and all 12 hard Skills.
- Deterministic canonical-to-install bundle synchronization and SHA-256 parity
  checks.
- Local install proof for Codex, Claude Code, and Cursor user-scope locations.
- The existing Archive Plate, six architecture diagrams, runtime, evidence,
  quality gate, and source fingerprint unchanged.

## Verify

```bash
npm run bundle:build
npm run media:build
npm run validate
npm run fingerprint
npm test
gh skill publish --dry-run
```

Source fingerprint:
`sha256:4736e707ef1e4a851cee104822598af6246b9f3a89038a42a261cced898ab448`.

Status remains **repository-prequalified**. Human Director qualification is
still open. This project is not Warren Buffett, is not affiliated with
Berkshire Hathaway, and is not financial advice.

Part of [Hall of Fame Studio](https://github.com/MrMaii/Hall-of-Fame-Studio).
